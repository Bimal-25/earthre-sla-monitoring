rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The browser never talks to Firestore directly. The deployed serverless
    // function uses its Google service account through the Admin SDK/client.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
