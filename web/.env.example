# Local Phase 2/3 Functions Framework API.
# Phase 4 production builds inject the real Cloud Run URL through the process environment;
# do not commit a production web/.env file.
VITE_API_BASE_URL=http://localhost:8080
